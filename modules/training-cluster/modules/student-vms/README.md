# Student VM
Creates a small VM in Hetzner for further labs (kind).

Adds all public keys which where uploaded to hetzner & generated key to all VMs root account. (vm module by Hetzer)
Create SSH key per user to reference in welcome file of the webshell.